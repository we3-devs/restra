import type { Metadata } from "next";import SeoPageView,{seoJsonLd} from "@/components/seo/SeoPage";import {absoluteUrl,seoPages} from "@/lib/seo-content";const page=seoPages["restaurant-management-system"];export const metadata:Metadata={title:page.title,description:page.description,alternates:{canonical:absoluteUrl("/restaurant-management-system")},openGraph:{title:page.title,description:page.description,url:absoluteUrl("/restaurant-management-system")}};export default function Page(){return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(seoJsonLd(page,"/restaurant-management-system"))}}/><SeoPageView page={page}/></>}